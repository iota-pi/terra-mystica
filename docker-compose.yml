---
version: "3.3"
services:
  application:
    image: ${DOCKER_MAIN}
    ports:
      - 8080
    volumes:
      - ./src:/${APP_NAME}
    working_dir: /${APP_NAME}

  poetry:
    image: ${PYTHON_POETRY}
    volumes:
      - .:/${APP_NAME}
    working_dir: /${APP_NAME}
